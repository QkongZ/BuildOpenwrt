# x86_64.config - 示例 .config 片段 (带中文注释)
# 这是针对 x86_64 平台的配置片段，实际项目中请根据需要调整

# 目标平台（示例项，编译系统会通过 make defconfig 解析）
# 你可以在此处启用/禁用内核模块、包等
CONFIG_TARGET_x86=y
CONFIG_TARGET_x86_64=y
# 示例：包含 luci Web 界面
CONFIG_PACKAGE_luci=y
CONFIG_PACKAGE_luci-ssl=y

# 示例：启用 ssh 服务
CONFIG_PACKAGE_openssh-sftp-server=y

# 图片/文件系统选项（示例）
# CONFIG_TARGET_IMAGES_GZIP is optional
# 这里直接保留相对通用的构建选项